<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- ⚠️ 少了这一条,点「开始同步」必定闪退:RelayService.onCreate 里用
         PowerManager.newWakeLock 拿了 PARTIAL_WAKE_LOCK 并 acquire(),而 acquire 在没有
         WAKE_LOCK 权限时抛 SecurityException —— 异常发生在 onCreate、没人接,整个进程当场
         死。表现是"点一下按钮 App 就没了",而且**服务从来没起来过**,所以 Mac 那边一条
         数据都收不到(配对本身是 Activity 里的事,所以会显示配对成功,更误导)。 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/ic_media_play"
        android:label="歌词中继"
        android:theme="@style/Theme.PhonlyricsRelay"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".RelayService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Required for MediaSessionManager.getActiveSessions -->
        <service
            android:name=".NotificationListener"
            android:exported="true"
            android:label="歌词中继通知读取"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
